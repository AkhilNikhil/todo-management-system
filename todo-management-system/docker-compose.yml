services:
  backend:
    image: todo-backend:v3
    container_name: todo-backend
    env_file:
      - .env
    ports:
      - "5000:5000"

  frontend:
    image: todo-frontend:v3
    container_name: todo-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
